import 'package:flutter/material.dart';

class VokabelPage extends StatelessWidget{

  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.blue,
      child: Column(
          children: <Widget>[
            Text("Momentum"),
          ]
      ),
    );
  }
}